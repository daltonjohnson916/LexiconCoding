say — prints text to the output box

ask — asks the user a question and stores their answer in a variable

set — sets a variable to a specific value

get — retrieves a variable’s value

add — adds a number or value to a variable

sub — subtracts a number from a variable

show — displays a variable’s value

hide — hides output temporarily

loop — repeats a block of code multiple times

print — displays text (same as say)

input — gets input from the user

start — begins a process

stop — stops a running process

wait — pauses the program for a moment

run — runs a command or block of code

clear — clears the screen or output

alert — shows a simple alert message

write — writes text or data

output — sends data to the output box

echo — repeats back what you say

display — shows something visually

askuser — asks a question with a popup

combine — joins two strings together

divide — divides one number by another

multiply — multiplies numbers

minus — subtracts values

plus — adds values

draw — creates a shape on screen

erase — removes a drawing

log — records text in a log

time — shows the current time

date — shows the current date

new — creates a new object or value

old — references a previous object

remember — stores data for later use

forget — clears saved data

list — creates a list variable

index — finds a position in a list

count — counts items in a list

sort — organizes items in order

join — combines list items into a string

split — breaks a string into parts

uppercase — makes text uppercase

lowercase — makes text lowercase

capitalize — capitalizes the first letter

reverse — flips a string backwards

find — looks for a word in text

replace — replaces part of a string

trim — removes extra spaces

length — gets how long a string is

random — gets a random number

range — creates a range of numbers

choose — picks a random item

option — defines an option to select

switch — handles multiple choices

case — defines an option inside switch

default — fallback value if no case matches

color — changes background color

background — sets background image or color

theme — switches between dark/light theme

music — plays background music

sound — plays a short sound

mute — turns off sound

unmute — turns sound back on

volume — changes sound level

play — starts playback

pause — pauses playback

stopmusic — stops playing music

resume — continues music or task

loopmusic — loops a track

path — sets or gets file path

dir — lists directory contents

file — refers to a file

rename — renames a file

delete — removes a file

copy — duplicates a file

move — moves a file to another folder

open — opens a file

close — closes a file

save — saves a file

connect — connects to a network or database

disconnect — disconnects from a network

ping — checks if something is reachable

message — sends a message

notify — shows a notification

error — shows an error message

warn — shows a warning message

success — shows a success message

info — shows information

debug — prints debug info

ready — signals when something is ready

loading — shows a loading message

done — signals completion

startapp — starts an application

stopapp — stops the current app

reboot — restarts the environment

shutdown — shuts down the program

restart — restarts the code

reload — reloads resources

sync — synchronizes data

send — sends data to another user or server

receive — receives incoming data

upload — uploads a file

download — downloads a file

zip — compresses files

unzip — extracts compressed files

encrypt — encodes data

decrypt — decodes data

compress — reduces file size

extract — unpacks files

drawline — draws a line

drawcircle — draws a circle

drawsquare — draws a square

fill — fills a shape with color

stroke — outlines a shape

text — draws text on screen

font — changes font type

size — changes text size

bold — makes text bold

italic — italicizes text

underline — underlines text

shadow — adds shadow to text

clearcanvas — clears all drawings

showcanvas — shows drawing area

hidecanvas — hides drawing area

cursor — changes cursor style

click — triggers a click event

hover — triggers hover event

scroll — scrolls content

zoom — zooms in/out

import — imports another Lexicon file

export — exports data

define — creates a constant or function

undefine — removes a definition

link — links two objects

unlink — removes a link

bind — connects event to action

unbind — removes event

update — refreshes something

refresh — reloads data

connectdb — connects to a database

query — runs a database query

insert — adds a new record

deletefrom — removes records from database

select — gets data

where — filters database results

and — logical AND

or — logical OR

not — logical NOT

true — true value

false — false value

yes — synonym for true

no — synonym for false

on — turns something on

off — turns something off

toggle — switches state

enable — enables a feature

disable — disables a feature

active — marks active state

inactive — marks inactive state

create — makes a new object

destroy — removes an object

spawn — creates an entity

despawn — removes an entity

enter — enters a scene

exit — leaves a scene

showtext — shows hidden text

hidetext — hides visible text

focus — focuses input

unfocus — removes focus

moveup — moves object up

movedown — moves object down

moveleft — moves object left

moveright — moves object right

jump — moves object upward quickly

sit — sets idle position

stand — returns to standing

walk — moves slowly

runfast — moves quickly

slow — slows down speed

record — starts recording

stoprecord — stops recording

capture — takes snapshot

screenshot — captures screen

frame — references frame number

scene — sets scene or environment

transition — fades between scenes

fadein — fades in screen

fadeout — fades out screen

blink — quick visibility toggle

flash — flashes screen

vibrate — shakes screen/device

notifyuser — sends system notification

soundon — turns on sound

soundoff — turns off sound

reset — resets system or variable

loopforever — runs endlessly

breakloop — exits loop

continue — skips to next loop cycle

exitloop — leaves loop fully

greet — says hello

farewell — says goodbye

thank — expresses thanks

apologize — says sorry

welcome — greets warmly

confirm — asks yes/no question

deny — rejects confirmation

explain — prints a reason

repeatafter — repeats given text

translate — translates text into another language

repeat — repeats a code block

call — runs a function

stop — halts function or code

start — begins a process

wait — delays execution

print — shows text or variable value

input — takes user input

run — runs another program or script

loop — repeats block multiple times

next — jumps to next item in loop

previous — goes to previous item

begin — starts section of code

end — closes code section

forward — moves object forward

backward — moves object backward

calculate — performs math

randomnum — gets random number

sleep — pauses temporarily

pause — same as sleep

resume — resumes paused process

execute — runs command

load — loads file

save — saves data

reset — restarts current script

refresh — reloads display

clear — clears content

display — shows result

animate — animates object

blink — quick flash effect

vibrate — vibrates window/device

slide — moves smoothly

grow — increases size

shrink — decreases size

fade — fade transition

rotate — rotates object

zoom — scales in or out

highlight — emphasizes text

dim — de-emphasizes text

brighten — increases brightness

shake — shakes window or object

clone — duplicates object

destroy — deletes object

replace — swaps object

update — refresh data

download — downloads file

upload — uploads file

connect — connects to server

disconnect — disconnects

encrypt — secures data

decrypt — decrypts data

filter — filters results

map — applies function to list

reduce — merges list items

insert — adds element

delete — removes element

merge — combines lists

split — splits text

join — joins text

combine — joins text or lists

push — adds item to list end

pop — removes last item

shift — removes first item

unshift — adds item to start

uppercase — converts to uppercase

lowercase — converts to lowercase

capitalize — capitalizes first letter

trim — removes spaces

pad — adds spaces

format — formats text

compare — compares two values

match — matches pattern

replace — replaces text

indexof — finds text position

startswith — checks prefix

endswith — checks suffix

contains — checks if inside string

slice — cuts text section

repeatstr — repeats text

shuffle — randomizes order

range — creates number range

sum — adds all numbers

avg — average of numbers

min — smallest number

max — largest number

count — counts items

unique — removes duplicates

difference — finds unique items

intersection — finds common items

union — merges all unique items

subtract — removes from list

modulo — finds remainder

divide — divides numbers

multiply — multiplies numbers

power — exponent math

sqrt — square root

abs — absolute value

round — rounds number

ceil — rounds up

floor — rounds down

sin — sine function

cos — cosine function

tan — tangent function

log — logarithm

exp — exponential

randomchoice — picks random item

choice — synonym for randomchoice

option — defines a selectable option

switch — runs matching option

case — defines specific option

default — fallback case

when — conditional trigger

otherwise — fallback block

each — runs through list items

for — basic for loop

while — loop until condition false

until — loop until true

break — stop loop

continue — skip loop cycle

return — gives back a value

exit — quits program

restart — restarts program

endloop — ends loop

waitfor — waits for event

animatein — fade in animation

animateout — fade out animation

transition — smooth visual change

move — moves object

scale — resizes object

rotate3d — rotates object in 3D

shakewindow — shakes window

flashscreen — flashes screen

notifysound — plays notification sound

messagebox — shows popup box

popup — creates popup

prompt — asks user input

confirmbox — asks yes/no

toast — shows quick message

alertbox — alert popup

errorbox — error popup

warnbox — warning popup

infobox — info popup

logevent — logs an event

debuglog — logs debug info

connectapi — connects to API

fetch — gets data from API

post — sends data to API

get — retrieves data from API

put — updates API data

deleteapi — deletes API data

openfile — opens local file

savefile — saves file

readfile — reads file contents

writefile — writes to file

reboot — restarts the IDE

shutdown — exits IDE

startapp — launches new app

stopapp — closes app

launch — runs new program

terminate — stops program

compile — compiles code

interpret — interprets code

tokenize — splits code into tokens

parse — analyzes code

build — creates program

install — installs component

uninstall — removes component

upgrade — updates software

rollback — reverts version

clean — deletes temp files

generate — creates file or data

deploy — sends project online

pushcode — pushes code to server

pullcode — pulls code from repo

syncdata — synchronizes files

backup — saves backup

restore — loads backup

recover — fixes corrupted data

scan — scans system or files

analyze — analyzes data

detect — detects something

measure — measures data

monitor — watches variable

logerror — logs errors

runprocess — starts system process

killprocess — ends process

sleepfor — waits for time

await — waits for async task

async — runs code asynchronously

schedule — sets timed task

timer — countdown timer

event — listens for event

trigger — fires event

handler — runs when event triggered

